<?php

declare(strict_types=1);

require __DIR__ . '/../vendor/autoload.php';

//? Las classes hijas de classes abstractas pueden tener los métodos abstractos de la clase padre

//? Las clases que implementan interfaces deben implementar todos los métodos de la interfaz

//* Las clases hijas de clases abstractas pueden tener las propiedades de la clase padre

//* Las clases que implementan interfaces deben tener las propiedades de la interfaz

//? Las clases hijas de clases abstractas pueden tener propiedades privadas y protegidas

//? Las clases que implementan interfaces no pueden tener propiedades privadas, solo pueden tener propiedades públicas

//* Las clases hijas de clases abstractas pueden extender una sola clase padre

//* Las clases que implementan interfaces pueden implementar múltiples interfaces